{% extends 'base.html.twig' %}

{% block title %}Guestbook{% endblock %}

{% block body %}
    <h1>Review</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ guestbook.id }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ guestbook.username }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td><a href="mailto:{{ guestbook.email }}">{{ guestbook.email }}</a></td>
            </tr>
            <tr>
                <th>Homepage</th>
                <td><a href="{{ guestbook.homepage }}" target="_blank">{{ guestbook.homepage }}</a></td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ guestbook.text }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('guestbook_index') }}">back to list</a>

    {#<a href="{{ path('guestbook_edit', {'id': guestbook.id}) }}">edit</a>#}

    {#{{ include('guestbook/_delete_form.html.twig') }}#}
{% endblock %}
